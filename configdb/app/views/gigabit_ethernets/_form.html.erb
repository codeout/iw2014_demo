<%= form_for(@gigabit_ethernet) do |f| %>
  <% if @gigabit_ethernet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gigabit_ethernet.errors.count, "error") %> prohibited this gigabit_ethernet from being saved:</h2>

      <ul>
      <% @gigabit_ethernet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :device_id %><br>
    <%= f.text_field :device_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :speed %><br>
    <%= f.text_area :speed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
