<%= form_for(@ipv4) do |f| %>
  <% if @ipv4.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ipv4.errors.count, "error") %> prohibited this ipv4 from being saved:</h2>

      <ul>
      <% @ipv4.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :gigabit_ethernet_id %><br>
    <%= f.text_field :gigabit_ethernet_id %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </div>
  <div class="field">
    <%= f.label :primary %><br>
    <%= f.check_box :primary %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
